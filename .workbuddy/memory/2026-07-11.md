# 2026-07-11 工作日志

## 图表重叠问题修复
- 将图例(legend)位置从顶部(top)移到底部(bottom)，避免与折点文字重叠
- 将折点文字位置从折线上方(point.y - 10)移到折线下方(point.y + 12)
- 将文字基线从 bottom 改为 top
- 图表容器高度从 360px 恢复为 400px（给底部图例留出空间）
- 所有7个图表统一调整

## 占比图表分母逻辑修改
- 区域线索数量占比趋势：分母从"全部线索总数"改为"筛选后的线索总数"（选中账号的线索总数）
- 首次通话意向度占比趋势：分母从"全部线索总数"改为"筛选后的线索总数"（选中账号+大区的线索总数）
- 修改内容：
  - 时间维度键收集从基于全部数据改为基于筛选后的数据
  - 多选大区/意向度时的汇总线分母改为筛选后总数
  - 多选账号时的汇总线分母改为筛选后总数
  - 单选/不选时的独立线分母改为筛选后总数
- 全选账号+全选区域时，占比显示为100%

## 图表顶部文字截断修复
- 为所有8个图表添加 `layout: { padding: { top: 20 } }` 配置
- 增加顶部padding，防止折点文字被canvas边界截断

## Excel导入日期解析修复
- 问题：XLSX.js读取Excel时，日期被解析为Excel内部序列号（数字），导致parseDate解析失败
- 修复：将 `XLSX.utils.sheet_to_json(firstSheet)` 改为 `XLSX.utils.sheet_to_json(firstSheet, { raw: false })`
- 影响：数据导入和账号消耗导入两个地方都修复了

## 图表卡片筛选功能
- 添加"筛选图表"下拉按钮（📊 筛选图表）
- 下拉框显示所有8个图表名称，可多选/全选
- 默认全部显示
- 取消勾选后，对应图表卡片隐藏（display: none）
- 按钮实时显示当前显示数量（如"显示 5/8 个图表"）
- 点击外部自动关闭下拉框

## 各账号线索数量趋势多选汇总
- 修改 aggregateByAccountAndTime 函数
- 多选账号时（selectedAccounts.length > 1），显示汇总数据（一条线）
- 汇总线标签为选中账号名称连接，或"全部账号"（当全选时）
- 单选或不选时，仍显示各账号独立线（多条线）
